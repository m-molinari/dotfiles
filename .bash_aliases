# list
#
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

# remove
#
alias rm='rm -i'

# UPDATES
#

# Archlinux
alias up='sudo pacman -Syu'
alias uy='yay -Syu'
#
# Debian
alias ud='sudo apt-get update && sudo apt-get upgrade'
